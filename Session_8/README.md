Day 6 (26/06/26) : Anu Tr

Python post conditional topics

Iterative statements
Should keep the indentation strictly for compiling
Definite loops and Indefinite Loops
	• A definite Loop consists of count/the user knows how much repeated the process will go and when the process ends
	Eg:
	
	• An indefinite loop is the user doesn't know the info about when the process will ends, as it needs a condition to satisfy for giving output.
	Eg:

While LOOP

Initialization, Condition and Increment or Decrement
Using relational operators
And for joining the relational operators, we need to use logical operators.
If while=true 
{
}
Else
{
}

This is the syntax and if needs to enter the while loop whwnever the true occurs and when it breaks, it goes outside of loop.


->if while condition is true, next loop works
-> need intialization,condition,increment,decrement

= is used for assigning
== is used for comparing

Outer loop is used for rows
Inner loop is used to print the data in the row

FOR LOOP

   syntax
     for I in range():


FUNCTIONS
Function is a set of instruction to complete a particular task which can be recalled for later purposes
 function uses def for defining
 
SYNTAX:

Def functionname():
      -----------------------
      -----------------------
      return


Built in Function : max,min,sum,etc

Range function: range()
Eg: range(5)-> 0 to 4
      range(1,5)-> 1,2,3,4,5

def add_modify():
  a + b == c
  print(f"{a} + {b} = {c}")
  
def add_modifying(a,b):
  c = a + b
  print("Sum = ",c)

a= int(input("Enter the first number :"))
b= int(input("Enter the second number :"))

add_modify()
add_modifying(5,2)

3.user defined function


Note:Python can be used to generate the audio transcript of a video

Actual parameters
Formal parameters

Four type of function
 No arguments no return
 arguments no return
 no arguments return
 arguments return